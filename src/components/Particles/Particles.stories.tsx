import { Story, Meta } from "@storybook/react";
import Particles from "./index";

export default {
  title: "Particles",
  component: Particles,
} as Meta;

export const Default: Story = () => <Particles />;
