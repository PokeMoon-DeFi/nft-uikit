import React, { FC } from "react";
import {
  DataGrid,
  GridColDef,
  GridValueGetterParams,
} from "@material-ui/data-grid";

// const columns: ColDef[] = [{ field: "id", headerName: "ID", width: 70 }];
const TableGrid = () => {};

export default TableGrid;
